# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
build
.next
out

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
server.log
server_test.log

# Local database and state
*.db
*.db-journal
*.db-wal

# Environment variables
.env
.env.local
.env.*.local

# AI Studio Specific
firebase-applet-config.json
firebase-blueprint.json
firestore.rules
DRAFT_firestore.rules

# Project specific artifacts
temp/
tmp_piper/
cache/
assets/
uploads/
renders/
exports/
data/
piper/
*.mp4
*.wav
*.mp3
*.png
*.jpg
*.jpeg
piper.tar.gz

# Voice Studio. Cloned-voice checkpoints follow the same "models live on local disk"
# rule as Piper's, and the samples are somebody's recorded voice — personal data that
# must never end up in the repository. registry.json and audit.jsonl name real users.
voices/
.venv-clone/

# OS files
.DS_Store
Thumbs.db

# Runtime logs
*.log
.gstack/

outputs/*.txt
outputs/**/*.json
policy*.yaml
**/__pycache__/
*.pyc

rife/
test_veer_voice.txt

# Audit artifacts — kept on disk, not in git.
# merged_file.md alone is 64MB and tripped GitHub's size warning.
audit_output/
